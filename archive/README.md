# Archive

This directory stores legacy experiment artifacts retained only for traceability.

For V5, `archive/` is **not** the canonical active workspace. Active planning and canonical references should live under `docs/v5/`, `STATUS.md`, and `records/`.
